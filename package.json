{
  "name": "hollow-run",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "An isometric maze chase built with PixiJS — guide the fox to the exit, one procedurally generated grove at a time.",
  "scripts": {
    "test": "node --test",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.20.1"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
